{
  "name": "@apidevelopers/trust-face-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Owned Trust Face verification kernel: quality gates, normalized embeddings, cosine similarity, versioned thresholds, and provider-neutral 1:1 decision signals.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
