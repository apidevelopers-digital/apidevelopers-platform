import assert from"node:assert/strict";import test from"node:test";
import{TRUST_FACE_E2E_SPATIAL_CANONICAL_V1,makeE2ESyntheticBatch,runE2ESpatialCanonicalSyntheticTraining}from"../src/e2e-spatial-canonical-v1.mjs";
test("e2e profile keeps canonical topology and non-biometric limits",()=>{assert.deepEqual(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.inputShape,{width:112,height:112,channels:3});assert.deepEqual(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.stageWidths,[64,96,160,256]);assert.deepEqual(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.stageDepths,[1,2,3,2]);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.blockCount,8);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.embeddingDim,512);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.angularMarginObjective,true);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.biometricBackboneReady,false);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.productionReady,false);assert.equal(TRUST_FACE_E2E_SPATIAL_CANONICAL_V1.realBiometricTrainingAuthorized,false)});
test("e2e fixture is deterministic 112x112 RGB synthetic data",()=>{let a=makeE2ESyntheticBatch({seed:17,classCount:3}),b=makeE2ESyntheticBatch({seed:17,classCount:3});assert.deepEqual(a,b);assert.equal(a.authorityBasis,"synthetic");assert.equal(a.samples.length,3);assert.equal(a.samples[0].pixels.length,37632)});
test("angular-margin gradient reaches all canonical blocks, spatial kernels and pixels",()=>{let r=runE2ESpatialCanonicalSyntheticTraining({seed:19,classCount:3,epochs:2,learningRate:.0002,scale:12,marginRadians:.15});assert.equal(r.blockCount,8);assert.deepEqual(r.stageWidths,[64,96,160,256]);assert.deepEqual(r.stageDepths,[1,2,3,2]);assert.equal(r.embeddingDim,512);assert.equal(r.allBlocksReceiveGradient,true);assert.ok(r.blockGradientNorms.every(v=>Number.isFinite(v)&&v>0));assert.equal(r.canonicalFeatureGradientObserved,true);assert.equal(r.spatialKernelGradientObserved,true);assert.equal(r.pixelGradientObserved,true);assert.equal(r.embeddingNormApproximatelyOne,true);assert.equal(r.modelUpdated,true);assert.equal(r.endToEndSyntheticGradientReady,true);assert.match(r.checkpointDigest,/^sha256:[0-9a-f]{64}$/);assert.equal(r.biometricBackboneReady,false);assert.equal(r.productionReady,false);assert.equal(r.realBiometricTrainingAuthorized,false)});
test("e2e synthetic training is deterministic",()=>{let c={seed:23,classCount:3,epochs:1,learningRate:.0002,scale:12,marginRadians:.15};assert.deepEqual(runE2ESpatialCanonicalSyntheticTraining(c),runE2ESpatialCanonicalSyntheticTraining(c))});
