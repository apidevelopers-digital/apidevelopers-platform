{
  "name": "@apidevelopers/registry",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only canonical registry for API Developers.digital components, capabilities, contracts and policies.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
