{
  "name": "@apidevelopers/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned shared contracts and explicit adapters for API Developers.digital.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/planning-execution-plan.mjs && node --check src/canonical-ids.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
