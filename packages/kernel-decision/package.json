{
  "name": "@apidevelopers/kernel-decision",
  "version": "0.2.0",
  "private": true,
  "description": "Tenant-bound deterministic advisory decision over governed planning reports.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs",
    "./governed": "./src/governed.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/governed.mjs && npm test"
  },
  "dependencies": {
    "@apidevelopers/contracts": "file:../contracts"
  },
  "engines": {
    "node": ">=22"
  }
}
