{
  "name": "@apidevelopers/subscription-core",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned subscription lifecycle, renewal and plan-change contracts for API Developers.digital.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/model.mjs && node --check src/repository.mjs && node --check src/service.mjs && node --check src/index.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
