{
  "name": "@apidevelopers/kernel-memory",
  "version": "0.1.0",
  "private": true,
  "description": "Append-only institutional memory for problems, plans, decisions, executions, outcomes and lessons.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
