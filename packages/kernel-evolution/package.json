{
  "name": "@apidevelopers/kernel-evolution",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic advisory evolution proposals derived from governed audit reports.",
  "type": "module",
  "exports": {
    ".": "./src/index.mjs",
    "./governed": "./src/governed.mjs"
  },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node --check src/index.mjs && node --check src/governed.mjs && npm test"
  },
  "dependencies": {
    "@apidevelopers/contracts": "0.1.0"
  },
  "engines": {
    "node": ">=22"
  }
}
