import assert from"node:assert/strict";import test from"node:test";import{createSiteFactoryDryRun}from"../src/planner.mjs";
test("report redacts internal identifiers and user identity",()=>{const r=createSiteFactoryDryRun({manifest:{site:{id:"x",domain:"example.test"}},hostingerInventory:{found:true,wordpressReady:true,websites:[{uid:"internal-id-secret",username:"hosting-user-secret",enabled:true,domains:["example.test"]}],wordpressInstallations:[{id:"software-id-secret",username:"hosting-user-secret",domain:"example.test",valid:true}]},wordpressDiscovery:{hasWpV2:true,hasPagesRoute:true},wordpressAuthentication:{validated:true,user:{id:9,slug:"admin-slug-secret",name:"Admin Name Secret",roles:["administrator"],canEditPages:true,canPublishPages:true}},pagePlan:{mode:"dry-run",writesEnabled:false,operations:[],totals:{create:0,update:0,noop:0}}});const s=JSON.stringify(r);for(const value of["internal-id-secret","software-id-secret","hosting-user-secret","admin-slug-secret","Admin Name Secret"])assert.equal(s.includes(value),false);assert.equal(r.safety.reportRedacted,true);assert.equal(r.readyForApply,false)});
