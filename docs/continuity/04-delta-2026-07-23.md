# Delta de continuidade — 23/07/2026

Este documento atualiza a reancoragem canônica de 16/07/2026 sem substituí-la.

## 1. Fonte canônica preservada

A base de programação continua sendo:

`REANCORAGEM_CANONICA_CONTINUIDADE_API_DEVELOPERS_2026-07-16.md`

Ela permanece responsável por:
- identidade e nomenclatura;
- arquitetura do Kernel;
- estratégia por pacotes `@apidevelopers/kernel-*`;
- ordem das ondas;
- ritmo de implementação;
- limites de execução;
- próxima ação da Onda 2.

## 2. Atualizações operacionais verificadas

### Repositório operacional atual
- organização: `apidevelopers-digital`
- repositório: `apidevelopers-platform`

A reancoragem de 16/07 registra `sitedauni/apidevelopers-platform`.
Essa diferença deve ser tratada como atualização operacional verificada, não como correção retroativa do documento original.

### Runner canônico
- nome: `igor-mac-runner`
- labels: `self-hosted`, `macOS`, `X64`

Configuração esperada:

```yaml
runs-on:
  - self-hosted
  - macOS
  - X64
```

Runners hospedados pelo GitHub não são o padrão desta operação.

### Planning Engine
O arquivo legado `scripts/lib/planning-engine.mjs` recebeu correção isolada e workflow próprio de validação.

Estado observado:
- sintaxe corrigida;
- smoke test de importação adicionado;
- CI executado com sucesso no runner self-hosted do Mac;
- nenhuma integração automática executada.

### Pull requests
- PR #16: aberto, tecnicamente validado, ainda sujeito a governança/revisão.
- PR #15: draft, grande e conflitado; não deve avançar até nova reconciliação.
- nenhum merge foi executado nesta continuidade.

## 3. Regra de continuidade

A programação continua seguindo a reancoragem de 16/07.

Este delta só deve ser consultado para:
- localizar o repositório operacional atual;
- usar o runner correto;
- entender o estado dos PRs;
- evitar refazer trabalho já concluído;
- preservar a distinção entre validado, pendente e executado.

## 4. Próximo passo permitido

Retomar a Onda 2 na branch de fundação, respeitando a ordem canônica:
1. concluir `@apidevelopers/kernel-planning`;
2. criar `@apidevelopers/kernel-decision`;
3. incluir manifesto, implementação, README, testes e validação;
4. trabalhar em lote;
5. sem merge e sem deploy automático.

## 5. Estado

- status: PREPARADO_PARA_CONTINUIDADE
- risco: R2
- documento_origem: reancoragem canônica de 16/07/2026
- natureza: atualização incremental
- merge: NÃO_EXECUTADO
- deploy: NÃO_EXECUTADO
