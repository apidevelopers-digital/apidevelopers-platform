name: Plan Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/plan-core/**"
      - ".github/workflows/plan-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/plan-core/**"
      - ".github/workflows/plan-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: plan-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check plan-core
        run: npm --prefix packages/plan-core run check
      - name: Check domain boundaries
        run: |
          node --check packages/limits-core/src/index.mjs
          node --check packages/usage-core/src/index.mjs
          node --check packages/registry-core/src/index.mjs
