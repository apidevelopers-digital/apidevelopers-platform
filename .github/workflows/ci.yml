name: Platform CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: platform-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Preflight workspace manifests
        run: node scripts/check-workspace-manifests.mjs
      - name: Install workspace links
        run: npm install --ignore-scripts --no-audit --no-fund --no-package-lock
      - name: Test root tooling
        run: node --test --test-reporter=spec tests/tooling/*.test.mjs
      - name: Test root integration
        run: node scripts/test-integration-sequentially.mjs
      - name: Test workspace packages
        run: node scripts/test-workspaces-sequentially.mjs
      - name: Run institutional audit
        run: npm run audit:institutional
      - name: Validate institutional audit artifacts
        run: |
          test -f .audit/snapshot.json
          test -f .audit/report.md
          node -e 'const fs=require("fs"); const s=JSON.parse(fs.readFileSync(".audit/snapshot.json","utf8")); if (!s.commit || !s.summary || typeof s.summary.files < 1) process.exit(1);'
      - name: Reject transient publishing artifacts
        run: |
          test ! -d .wave5
          test ! -d .publish
