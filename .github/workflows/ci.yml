name: Platform CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: platform-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install workspace links
        run: npm install --ignore-scripts --no-audit --no-fund --no-package-lock
      - name: Run complete platform test suite
        run: npm test
      - name: Run institutional audit
        run: npm run audit:institutional
      - name: Validate institutional audit artifacts
        run: |
          test -f .audit/snapshot.json
          test -f .audit/report.md
          node -e 'const fs=require("fs"); const s=JSON.parse(fs.readFileSync(".audit/snapshot.json","utf8")); if (!s.commit || !s.summary || typeof s.summary.files !== "number" || summary.files < 1) process.exit(1);'
      - name: Reject transient publishing artifacts
        run: |
          test ! -d .wave5
          test ! -d .publish
