name: SaaS Runtime CI

on:
  pull_request:
    paths:
      - "packages/saas-runtime/**"
      - "packages/contracts/src/saas-*.mjs"
      - "packages/persistence-core/**"
      - ".github/workflows/saas-runtime-ci.yml"
  push:
    branches: [main]
    paths:
      - "packages/saas-runtime/**"
      - "packages/contracts/src/saas-*.mjs"
      - "packages/persistence-core/**"
      - ".github/workflows/saas-runtime-ci.yml"

jobs:
  saas-runtime:
    name: SaaS runtime gate
    runs-on:
      - self-hosted
      - macOS
      - X64
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check SaaS runtime
        working-directory: packages/saas-runtime
        shell: bash
        run: |
          set +e
          npm run check 2>&1 | tee /tmp/saas-runtime-check.log
          status=${PIPESTATUS[0]}
          if [ "$status" -ne 0 ]; then
            tail -n 40 /tmp/saas-runtime-check.log | sed ':a;N;$!ba;s/%/%25/g;s/\r/%0D/g;s/\n/%0A/g' | xargs -0 -I{} echo "::error title=SaaS runtime failure::{}"
            exit "$status"
          fi
