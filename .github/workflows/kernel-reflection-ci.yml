name: Kernel Reflection CI

on:
  pull_request:
    paths:
      - "packages/kernel-reflection/**"
      - "packages/contracts/**"
      - ".github/workflows/kernel-reflection-ci.yml"
  push:
    branches: [main]
    paths:
      - "packages/kernel-reflection/**"
      - "packages/contracts/**"
      - ".github/workflows/kernel-reflection-ci.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: kernel-reflection-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reflection:
    name: Kernel Reflection gate
    runs-on:
      group: organization-macos-ci
      labels: [self-hosted, macOS, X64]
    timeout-minutes: 15
    defaults:
      run:
        working-directory: packages/kernel-reflection
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install dependencies
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Diagnose syntax
        shell: bash
        run: |
          set -euo pipefail
          while IFS= read -r file; do
            echo "checking syntax: $file"
            node --check "$file"
          done < <(find src test -type f -name '*.mjs' | sort)
      - name: Validate kernel reflection
        shell: bash
        run: |
          set -uo pipefail
          set +e
          npm run check 2>&1 | tee kernel-reflection-gate.log
          status=${PIPESTATUS[0]}
          set -e
          if [[ "$status" -ne 0 ]]; then
            key_lines="$(grep -E 'Cannot find|ERR_MODULE_NOT_FOUND|Error \[' kernel-reflection-gate.log || true)"
            if [[ -z "$key_lines" ]]; then
              key_lines="$(tail -n 20 kernel-reflection-gate.log)"
            fi
            while IFS= read -r line; do
              echo "::error::$line"
            done <<< "$key_lines"
            exit "$status"
          fi
          grep -Fq "KERNEL_REFLECTION_GATE_OK" kernel-reflection-gate.log
