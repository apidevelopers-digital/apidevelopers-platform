name: Checkout Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/checkout-core/**"
      - ".github/workflows/checkout-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/checkout-core/**"
      - ".github/workflows/checkout-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: checkout-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check checkout-core
        run: npm --prefix packages/checkout-core run check
      - name: Check domain boundaries
        run: |
          node --check packages/plan-core/src/index.mjs
          node --check packages/subscription-core/src/index.mjs
          node --check packages/billing-core/src/index.mjs
          node --check packages/persistence-core/src/index.mjs
