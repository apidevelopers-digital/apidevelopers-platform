name: API Key Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/apikey-core/**"
      - ".github/workflows/apikey-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/apikey-core/**"
      - ".github/workflows/apikey-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: apikey-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check apikey-core baseline
        run: npm --prefix packages/apikey-core run check
