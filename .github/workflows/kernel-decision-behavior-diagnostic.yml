name: Kernel Decision Install Diagnostic

on:
  pull_request:
    paths:
      - "packages/kernel-decision/**"
      - "packages/contracts/**"
      - ".github/workflows/kernel-decision-behavior-diagnostic.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  install:
    runs-on:
      group: organization-macos-ci
      labels: [self-hosted, macOS, X64]
    timeout-minutes: 15
    defaults:
      run:
        working-directory: packages/kernel-decision
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install local dependencies only
        run: npm install --ignore-scripts --no-audit --no-fund
