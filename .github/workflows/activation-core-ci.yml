name: Activation Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/activation-core/**"
      - ".github/workflows/activation-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/activation-core/**"
      - ".github/workflows/activation-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: activation-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check activation-core
        run: npm --prefix packages/activation-core run check
      - name: Check domain boundaries
        run: |
          node --check packages/checkout-core/src/index.mjs
          node --check packages/subscription-core/src/index.mjs
          node --check packages/provisioning-core/src/index.mjs
          node --check packages/persistence-core/src/index.mjs
