name: Billing Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/billing-core/**"
      - ".github/workflows/billing-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/billing-core/**"
      - ".github/workflows/billing-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: billing-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check billing-core
        run: npm --prefix packages/billing-core run check
      - name: Check domain boundaries
        run: |
          node --check packages/plan-core/src/index.mjs
          node --check packages/usage-core/src/index.mjs
          node --check packages/subscription-core/src/index.mjs
          node --check packages/persistence-core/src/index.mjs
