name: Contracts Auth Smoke CI

on:
  push:
    branches:
      - "foundation/**"
    paths:
      - "packages/contracts/**"
      - ".github/workflows/contracts-auth-smoke-ci.yml"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check auth contract
        run: node --check packages/contracts/src/auth-context.mjs
      - name: Test auth contract
        run: node --test packages/contracts/test/auth-context.test.mjs
