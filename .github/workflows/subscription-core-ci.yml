name: Subscription Core CI

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - "foundation/**"
    paths:
      - "packages/subscription-core/**"
      - ".github/workflows/subscription-core-ci.yml"
  pull_request:
    branches:
      - main
    paths:
      - "packages/subscription-core/**"
      - ".github/workflows/subscription-core-ci.yml"

permissions:
  contents: read

concurrency:
  group: subscription-core-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: [self-hosted, macOS, X64]
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check subscription-core
        run: npm --prefix packages/subscription-core run check
      - name: Check domain boundaries
        run: |
          node --check packages/plan-core/src/index.mjs
          node --check packages/entitlement-core/src/index.mjs
          node --check packages/persistence-core/src/index.mjs
          node --check packages/tenant-core/src/index.mjs
